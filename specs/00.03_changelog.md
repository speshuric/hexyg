### 2026-01-23
- Инициализирован Rust Workspace в корневом `Cargo.toml`.
- Создана структура папок для `crates/hexyg-core`, `crates/hexyg-cli`, `crates/hexyg-lsp`.
- Созданы базовые `Cargo.toml` и заглушки исходного кода для всех Rust-крейтов.
- Исправлены ошибки сборки, связанные с пустым файлом конфигурации Cargo.

### 2026-01-24
- Исправлено правило `preview` в грамматике `grammar/hexyg.g4` для корректной работы с токеном `PREVIEW_LITERAL`.
- Создан эталонный тестовый файл `tests/corpus/valid/01_reference.hex` на основе спецификации.
- Добавлен раздел "Grammar Development" в `CONTRIBUTING.md` с описанием процесса валидации грамматики.
- Создан крейт `crates/hexyg-grammar-check` для автоматической проверки корректности ANTLR грамматики и валидации тестовых файлов.
- Исправлены критические ошибки в грамматике:
  - Переупорядочены лексерные правила (комментарии и PREVIEW_LITERAL должны матчиться первыми)
  - Устранён конфликт между HEX_BYTE и ADDRESS токенами через введение универсального HEX_SEQUENCE
  - Убран символ `:` из UNQUOTED_VALUE для корректного парсинга struct полей
  - Изменена логика валидации: ambiguity warnings допустимы, критичны только syntax errors
- Добавлена документация команды `cargo test -p hexyg-grammar-check` в README.md и README.ru.md
